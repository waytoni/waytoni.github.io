name: Run Python Script on Text File Update

on:
  push:
    paths:
      - 'Abhidharma_Aruth/Abhidharma_Aruth_B_youtube_links.txt' # Update with the actual path to your text file
      - 'Abhidharma_Aruth/Abhidharma_Aruth_youtube_links.txt'
jobs:
  run-python-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.11 # Set your desired Python version

      - name: Run Python Script
        run: python Abhidharma_Aruth/read_AA_youtube_links_draft.py # Update with the actual path to your Python script
